// Trading 212 API Plugin - Skills-only plugin
// Skills are loaded from the ./plugins/trading212-api/skills directory

export default function register(api: any) {
  api.logger?.info?.("Trading 212 API plugin loaded (skills-only)");
}
